import React from 'react';
import { Container, Typography } from '@mui/material';

const DepthChart = () => {
  return (
    <Container>
      <Typography variant="h4">Depth Chart</Typography>
      <Typography>View and manage your depth chart.</Typography>
    </Container>
  );
};

export default DepthChart;
