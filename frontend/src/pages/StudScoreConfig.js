import React from 'react';
import { Container, Typography } from '@mui/material';

const StudScoreConfig = () => {
  return (
    <Container>
      <Typography variant="h4">Stud Score Configuration</Typography>
      <Typography>Configure attribute weights for different positions.</Typography>
    </Container>
  );
};

export default StudScoreConfig;
